#include "main.h"

/**
 * mul - multiply two intergers.
 *
 * Return: multiplication between two integers.
 * @a: is an interger.
 * @b: is an integer.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
