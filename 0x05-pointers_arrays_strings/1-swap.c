#include "main.h"

/**
 * swap_int - swap the values of two integers
 *
 *@a: is an integer
 *@b: is an integer
 *
 * Return: 0
 */
int swap_int(int *a, int *b)
{
	int c, d;

	c = 42;	
	d = 98;
	*a = c;
	*b = d;
	return (0);
}
